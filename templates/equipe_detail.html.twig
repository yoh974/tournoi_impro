{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <h1>{{ equipe.getNomEquipe() }}</h1>
                <div class="image_equipe_detail d-flex justify-content-center" style="max-width: 100%;">
                    <img src="{{ equipe.getImage() }}" alt="">
                </div>
            </div>
            <div class="col-md-8">
                
            </div>
        </div>
    </div>

{% endblock %}

{% block title %}

{% endblock %}

